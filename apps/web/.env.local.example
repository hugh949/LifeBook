# Use /api so Next rewrites to backend (local dev: backend at localhost:8000)
NEXT_PUBLIC_API_BASE_URL=/api
NEXT_PUBLIC_DEFAULT_LANGUAGE=auto
NEXT_PUBLIC_SUPPORTED_LANGUAGES=en,zh-yue,zh,es,ur

# Proxy target when running web without Docker (npm run dev). Default is http://localhost:8000.
# Copy to .env.local and set if the API runs elsewhere.
# API_UPSTREAM=http://localhost:8000
