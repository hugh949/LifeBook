# Use /api so Next rewrites to backend (local dev: backend at localhost:8000)
NEXT_PUBLIC_API_BASE_URL=/api
NEXT_PUBLIC_DEFAULT_LANGUAGE=auto
NEXT_PUBLIC_SUPPORTED_LANGUAGES=en,zh-yue,zh,es,ur

# Proxy target (local dev or production). Default is http://localhost:8000.
# Must be the API base URL only — no path (e.g. https://your-api.azurecontainerapps.io).
# The proxy appends paths like /voice/stories/shared/delete to this base.
# API_UPSTREAM=http://localhost:8000

# Narration BGM: base path for mood-based tracks (default /bg-narrate -> /bg-narrate-reflective.mp3 etc.)
# NEXT_PUBLIC_NARRATE_BGM_BASE=/bg-narrate
