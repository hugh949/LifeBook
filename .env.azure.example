# Azure DB: run the narration voice log dump against production (e.g. to debug clone voice).
# 1. Copy this file:  cp .env.azure.example .env.azure
# 2. Set DATABASE_URL to your Azure Postgres connection string in .env.azure
# 3. Run the dump (from repo root):
#
#    docker compose run --rm --env-file .env.azure api python /app/scripts/dump_narration_voice_log.py --name Harry
#
# Or use the helper script:
#
#    ./scripts/dump_narration_voice_azure.sh
#    ./scripts/dump_narration_voice_azure.sh Harry
#
# Output goes to stdout; redirect to save:  ./scripts/dump_narration_voice_azure.sh > azure_harry_narration.log 2>&1

# Required: Azure Postgres connection string (overrides local DATABASE_URL when using --env-file .env.azure)
DATABASE_URL=postgresql://USER:PASSWORD@YOUR-SERVER.postgres.database.azure.com:5432/YOUR-DB?sslmode=require

# Optional: set if you want the dump to report whether ElevenLabs is configured in Azure (value not printed)
# ELEVENLABS_API_KEY=
