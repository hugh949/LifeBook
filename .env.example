# Copy to .env — no secrets needed for local run.
# For Azure deploy, set these in the Azure resource (App Service / Container Apps) or in GitHub Actions env.

DATABASE_URL=postgresql://lifebook:lifebook@db:5432/lifebook
OPENAI_API_KEY=
APP_ENV=local
CORS_ALLOW_ORIGINS=http://localhost:3000

# Azure Blob Storage (optional; when set, uploads and photo URLs use real Azure)
AZURE_STORAGE_ACCOUNT=
AZURE_STORAGE_ACCOUNT_KEY=
AZURE_STORAGE_CONTAINER_PHOTOS=photos
AZURE_STORAGE_CONTAINER_AUDIO=audio
AZURE_STORAGE_SAS_TTL_MINUTES=15
AZURE_STORAGE_READ_SAS_TTL_MINUTES=60
